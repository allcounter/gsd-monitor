---
phase: 06-theme-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - GSDMonitor/Views/Settings/EditorSettingsView.swift
  - GSDMonitor/Views/Dashboard/PhaseDetailView.swift
  - GSDMonitor/Views/SidebarView.swift
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All .foregroundStyle(.secondary) instances replaced with Theme.textSecondary — zero system color remnants"
    - "Sidebar selected project row shows bg2 (#504945) background highlight"
  artifacts:
    - path: "GSDMonitor/Views/Settings/EditorSettingsView.swift"
      provides: "Fully theme-migrated editor settings"
      contains: "Theme.textSecondary"
    - path: "GSDMonitor/Views/Dashboard/PhaseDetailView.swift"
      provides: "Fully theme-migrated phase detail"
      contains: "Theme.textSecondary"
    - path: "GSDMonitor/Views/SidebarView.swift"
      provides: "Sidebar with bg2 selection highlight"
      contains: "listRowBackground"
  key_links:
    - from: "GSDMonitor/Views/SidebarView.swift"
      to: "GSDMonitor/Theme/Theme.swift"
      via: "Theme.surfaceHover for selection background"
      pattern: "Theme\\.surfaceHover"
---

<objective>
Close two verification gaps from Phase 06 VERIFICATION.md:
1. Replace 3 remaining .foregroundStyle(.secondary) instances with Theme.textSecondary
2. Add sidebar list row selection highlighting using Theme.surfaceHover (bg2)

Purpose: Achieve 5/5 truth verification for Phase 06 — zero system colors and themed sidebar selection.
Output: Three files updated, both gaps closed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GSDMonitor/Theme/Theme.swift
@GSDMonitor/Views/Settings/EditorSettingsView.swift
@GSDMonitor/Views/Dashboard/PhaseDetailView.swift
@GSDMonitor/Views/SidebarView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace .foregroundStyle(.secondary) with Theme.textSecondary</name>
  <files>
    GSDMonitor/Views/Settings/EditorSettingsView.swift
    GSDMonitor/Views/Dashboard/PhaseDetailView.swift
  </files>
  <action>
    Replace all 3 remaining instances of .foregroundStyle(.secondary) with .foregroundStyle(Theme.textSecondary):

    **EditorSettingsView.swift:**
    - Line 21: `.foregroundStyle(.secondary)` → `.foregroundStyle(Theme.textSecondary)` (empty editors message)
    - Line 28: `.foregroundStyle(.secondary)` → `.foregroundStyle(Theme.textSecondary)` (editor path caption)

    **PhaseDetailView.swift:**
    - Line 77: `.foregroundStyle(.secondary)` → `.foregroundStyle(Theme.textSecondary)` ("No plans found" message)

    These are simple find-replace operations. Do NOT change any other code in these files.
  </action>
  <verify>
    Run: grep -rn "\.foregroundStyle(\.secondary)" GSDMonitor/
    Expected: Zero matches. All .secondary usages eliminated.
  </verify>
  <done>Zero instances of .foregroundStyle(.secondary) remain in the entire GSDMonitor/ directory.</done>
</task>

<task type="auto">
  <name>Task 2: Add sidebar selection highlighting with bg2 background</name>
  <files>
    GSDMonitor/Views/SidebarView.swift
  </files>
  <action>
    Add .listRowBackground modifier to ProjectRow items inside the List in SidebarView.

    In the `projectSection(for:)` function, after `.tag(project.id)` on the ProjectRow, add:

    ```swift
    .listRowBackground(
        selectedProjectID == project.id
            ? Theme.surfaceHover
            : Color.clear
    )
    ```

    This requires access to `selectedProjectID` inside `projectSection`. Since `selectedProjectID` is a `@Binding var` on SidebarView, it's already accessible from `projectSection(for:)` as an instance method.

    Theme.surfaceHover maps to bg2 (#504945) which provides a subtle warm highlight for the selected sidebar row, matching the locked decision from CONTEXT.md.
  </action>
  <verify>
    Run: grep -n "listRowBackground" GSDMonitor/Views/SidebarView.swift
    Expected: At least one match showing Theme.surfaceHover usage.

    Run: grep -n "Theme.surfaceHover" GSDMonitor/Views/SidebarView.swift
    Expected: At least one match confirming bg2 color is used for selection.
  </verify>
  <done>Sidebar project rows show bg2 (#504945) background when selected, clear background when not selected.</done>
</task>

</tasks>

<verification>
1. `grep -rn "\.foregroundStyle(\.secondary)" GSDMonitor/` returns zero matches
2. `grep -n "listRowBackground" GSDMonitor/Views/SidebarView.swift` shows Theme.surfaceHover usage
3. Project builds without errors (xcodebuild or Xcode Cmd+B)
4. Both verification gaps from 06-VERIFICATION.md are resolved
</verification>

<success_criteria>
- Zero .foregroundStyle(.secondary) instances in codebase
- SidebarView implements .listRowBackground with Theme.surfaceHover for selected state
- Project compiles without errors
- Phase 06 re-verification would score 5/5 truths
</success_criteria>

<output>
After completion, create `.planning/phases/06-theme-foundation/06-03-SUMMARY.md`
</output>
