---
phase: 07-animated-progress-rings
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - GSDMonitor/Views/SidebarView.swift
autonomous: false

must_haves:
  truths:
    - "Sidebar project rows display a mini circular progress ring"
    - "Mini ring replaces the linear ProgressView in ProjectRow"
    - "No performance degradation with 10+ visible rings on screen"
  artifacts:
    - path: "GSDMonitor/Views/SidebarView.swift"
      provides: "Sidebar with mini progress rings in project rows"
      contains: "CircularProgressRing"
  key_links:
    - from: "GSDMonitor/Views/SidebarView.swift"
      to: "GSDMonitor/Views/Components/CircularProgressRing.swift"
      via: "CircularProgressRing mini usage"
      pattern: "CircularProgressRing\\("
---

<objective>
Add mini circular progress rings to sidebar project rows, replacing the linear ProgressView.

Purpose: Fulfills PROG-03 (sidebar mini rings). Uses the CircularProgressRing component created in Plan 01 with smaller dimensions (24x24, 3px line). Includes visual verification checkpoint.
Output: Updated SidebarView.swift with mini rings
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-animated-progress-rings/07-01-SUMMARY.md
@GSDMonitor/Views/SidebarView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace linear ProgressView with mini CircularProgressRing in ProjectRow</name>
  <files>GSDMonitor/Views/SidebarView.swift</files>
  <action>
In the `ProjectRow` struct (private struct at bottom of SidebarView.swift), replace the linear ProgressView with a mini CircularProgressRing.

Replace the current ProjectRow body:
```swift
var body: some View {
    VStack(alignment: .leading, spacing: 4) {
        Text(project.name)
            .font(.body)

        if let roadmap = project.roadmap {
            ProgressView(value: progressValue(roadmap: roadmap), total: 1.0)
                .progressViewStyle(.linear)
                .tint(Theme.accent)
        }
    }
    ...
}
```

With an HStack layout placing the mini ring to the right of the project name:
```swift
var body: some View {
    HStack(spacing: 8) {
        Text(project.name)
            .font(.body)

        Spacer()

        if let roadmap = project.roadmap {
            CircularProgressRing(
                progress: progressValue(roadmap: roadmap),
                ringColor: Theme.accent,
                lineWidth: 3
            )
            .frame(width: 24, height: 24)
        }
    }
    .contextMenu {
        if isManuallyAdded {
            Button("Remove from GSD Monitor", role: .destructive) {
                onRemove()
            }
        }
    }
}
```

Key details:
- Mini ring size: 24x24 frame, 3px lineWidth
- Color: Theme.accent (brightAqua) — same as current linear bar tint
- Background ring: Theme.bg2 (default from component)
- No percentage text overlay (too small at 24px)
- Keep existing progressValue(roadmap:) function unchanged
- Keep existing contextMenu unchanged
  </action>
  <verify>Build succeeds. Run app — sidebar project rows show mini circular ring on the right side instead of linear bar below the name. Ring animates on appear.</verify>
  <done>Sidebar ProjectRow displays mini CircularProgressRing (24x24, 3px line) aligned right of project name. Linear ProgressView removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of progress rings</name>
  <action>Human verifies that circular progress rings render correctly in both phase cards and sidebar.</action>
  <what-built>Circular progress rings in phase cards (80x80) and sidebar project rows (24x24), with smooth one-time animation on appear</what-built>
  <how-to-verify>
    1. Open GSD Monitor app
    2. Verify phase cards show circular progress rings (not linear bars)
    3. Verify ring starts at top (12 o'clock) and fills clockwise
    4. Verify percentage text is centered inside phase card rings
    5. Verify ring color: gray=not started, yellow=in progress, green=done
    6. Verify sidebar project rows show small circular rings on the right
    7. Verify rings animate smoothly on first view appearance
    8. Edit a file in a monitored project — verify rings do NOT re-animate (progress updates silently)
    9. Scroll through projects — verify no stuttering or frame drops with multiple rings visible
  </how-to-verify>
  <verify>Human confirms all 9 checks pass</verify>
  <done>Progress rings visually verified in both locations with correct animation behavior</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `swift build` passes
2. Sidebar project rows show mini circular ring (24x24) instead of linear bar
3. Mini ring uses Theme.accent color with Theme.bg2 background
4. Ring animates once on appear, not on data updates
5. No performance issues with 10+ projects visible in sidebar
6. All success criteria from Phase 7 roadmap met
</verification>

<success_criteria>
- SidebarView ProjectRow uses CircularProgressRing instead of linear ProgressView
- Mini ring is 24x24 with 3px line width
- Human verified: both phase card and sidebar rings look correct
- No performance degradation with multiple rings
</success_criteria>

<output>
After completion, create `.planning/phases/07-animated-progress-rings/07-02-SUMMARY.md`
</output>
