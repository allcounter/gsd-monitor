---
phase: 10-enhanced-empty-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GSDMonitor/Views/SidebarView.swift
  - GSDMonitor/Views/DetailView.swift
autonomous: true
requirements:
  - VISL-03

must_haves:
  truths:
    - "Empty sidebar shows themed ContentUnavailableView with Gruvbox colors and folder icon"
    - "No-selection detail view shows themed ContentUnavailableView with sidebar icon"
    - "No-roadmap detail view shows themed ContentUnavailableView with document icon"
    - "Search no-match state shows themed ContentUnavailableView with magnifyingglass icon"
    - "All empty states use Theme.fg1 for title, Theme.fg4 for description, Theme.textSecondary for icon"
  artifacts:
    - path: "GSDMonitor/Views/SidebarView.swift"
      provides: "Themed empty states for sidebar (no projects + no matching projects)"
      contains: "Theme.fg1"
    - path: "GSDMonitor/Views/DetailView.swift"
      provides: "Themed empty states for detail view (no selection + no roadmap)"
      contains: "Theme.fg1"
  key_links:
    - from: "GSDMonitor/Views/SidebarView.swift"
      to: "GSDMonitor/Theme/Theme.swift"
      via: "Theme.fg1, Theme.fg4, Theme.textSecondary color references"
      pattern: "Theme\\.(fg1|fg4|textSecondary)"
    - from: "GSDMonitor/Views/DetailView.swift"
      to: "GSDMonitor/Theme/Theme.swift"
      via: "Theme.fg1, Theme.fg4, Theme.textSecondary color references"
      pattern: "Theme\\.(fg1|fg4|textSecondary)"
---

<objective>
Replace all simple-init ContentUnavailableView empty states with closure-based initializers that apply Gruvbox theme colors. Covers four locations: sidebar empty (no projects), sidebar no-match (search filter), detail no-selection, and detail no-roadmap.

Purpose: Ensure empty states match the Gruvbox Dark visual identity established in Phase 6, rather than using default system colors.
Output: Themed empty states across SidebarView.swift and DetailView.swift.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GSDMonitor/Views/SidebarView.swift
@GSDMonitor/Views/DetailView.swift
@GSDMonitor/Theme/Theme.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme all empty states with Gruvbox colors using closure-based ContentUnavailableView</name>
  <files>GSDMonitor/Views/SidebarView.swift, GSDMonitor/Views/DetailView.swift</files>
  <action>
Replace each ContentUnavailableView with closure-based initializer applying Gruvbox colors. Color scheme for all empty states: Theme.fg1 for title text, Theme.textSecondary for icon, Theme.fg4 for description text.

**SidebarView.swift — emptyState (lines 69-75):**
Replace simple init with closure-based:
```swift
private var emptyState: some View {
    ContentUnavailableView {
        Label {
            Text("No Projects Found")
                .foregroundStyle(Theme.fg1)
        } icon: {
            Image(systemName: "folder.badge.questionmark")
                .foregroundStyle(Theme.textSecondary)
        }
    } description: {
        Text("GSD projects will appear here when discovered in ~/Developer")
            .foregroundStyle(Theme.fg4)
    }
}
```

**SidebarView.swift — noMatchingProjectsState (lines 77-82):**
Replace simple init with closure-based:
```swift
private var noMatchingProjectsState: some View {
    ContentUnavailableView {
        Label {
            Text("No Matching Projects")
                .foregroundStyle(Theme.fg1)
        } icon: {
            Image(systemName: "magnifyingglass")
                .foregroundStyle(Theme.textSecondary)
        }
    } description: {
        Text("Try adjusting your search or filter")
            .foregroundStyle(Theme.fg4)
    }
}
```

**DetailView.swift — no selection state (lines 106-110):**
Replace simple init with closure-based:
```swift
ContentUnavailableView {
    Label {
        Text("Select a Project")
            .foregroundStyle(Theme.fg1)
    } icon: {
        Image(systemName: "sidebar.left")
            .foregroundStyle(Theme.textSecondary)
    }
} description: {
    Text("Choose a project from the sidebar to view its roadmap")
        .foregroundStyle(Theme.fg4)
}
```

**DetailView.swift — no roadmap state (lines 95-100):**
Replace the plain `Text("No roadmap data available")` with a proper themed ContentUnavailableView:
```swift
ContentUnavailableView {
    Label {
        Text("No Roadmap Data")
            .foregroundStyle(Theme.fg1)
    } icon: {
        Image(systemName: "doc.text.magnifyingglass")
            .foregroundStyle(Theme.textSecondary)
    }
} description: {
    Text("This project doesn't have a ROADMAP.md file yet")
        .foregroundStyle(Theme.fg4)
}
```
Wrap this in a container that fills available space (Spacer or frame) to center it properly, replacing the old Text + frame + padding pattern.
  </action>
  <verify>Build with `swift build` or Xcode. Grep for `ContentUnavailableView(` (simple init) — should return zero matches. Grep for `Theme.fg1` in both files — should find matches in empty state code.</verify>
  <done>All four empty states use closure-based ContentUnavailableView with Theme.fg1 title, Theme.textSecondary icon, and Theme.fg4 description. No simple-init ContentUnavailableView remains in codebase.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'ContentUnavailableView(' GSDMonitor/Views/SidebarView.swift GSDMonitor/Views/DetailView.swift | grep -v '{'` — should return empty (no simple inits left)
2. `grep -rn 'Theme.fg1' GSDMonitor/Views/SidebarView.swift GSDMonitor/Views/DetailView.swift` — should show matches in empty state computed properties
3. Build succeeds without errors
4. Visual check: empty states show warm Gruvbox cream/brown text instead of system blue/gray
</verification>

<success_criteria>
- All four empty states (sidebar empty, sidebar no-match, detail no-selection, detail no-roadmap) use themed ContentUnavailableView
- Colors: Theme.fg1 (cream) for titles, Theme.textSecondary (gray-brown) for icons, Theme.fg4 (muted brown) for descriptions
- No simple-init ContentUnavailableView calls remain in SidebarView or DetailView
- App builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-enhanced-empty-states/10-01-SUMMARY.md`
</output>
