---
phase: 12-milestone-timeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GSDMonitor/Models/Roadmap.swift
  - GSDMonitor/Services/RoadmapParser.swift
autonomous: true
requirements:
  - DASH-03
must_haves:
  truths:
    - "Roadmap model contains milestones array grouping phases by milestone"
    - "RoadmapParser extracts milestone names and phase ranges from ## Milestones section"
    - "Completed milestones have isComplete = true derived from phase statuses"
    - "Existing Codable conformance is unbroken — milestones excluded from CodingKeys"
  artifacts:
    - path: "GSDMonitor/Models/Roadmap.swift"
      provides: "Milestone struct and roadmap.milestones field"
      contains: "struct Milestone"
    - path: "GSDMonitor/Services/RoadmapParser.swift"
      provides: "parseMilestones function populating roadmap.milestones"
      contains: "parseMilestones"
  key_links:
    - from: "GSDMonitor/Services/RoadmapParser.swift"
      to: "GSDMonitor/Models/Roadmap.swift"
      via: "parseMilestones returns [Milestone] used in Roadmap init"
      pattern: "milestones.*parseMilestones"
---

<objective>
Add Milestone data model and parser support for grouping phases by milestone.

Purpose: The timeline views (Plan 02) need milestone grouping data to render collapsed/expanded milestone sections. The current Roadmap model has a flat phases array with no milestone concept.
Output: Milestone struct in Roadmap.swift, parseMilestones in RoadmapParser.swift populating roadmap.milestones from ROADMAP.md ## Milestones section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GSDMonitor/Models/Roadmap.swift
@GSDMonitor/Models/Phase.swift
@GSDMonitor/Services/RoadmapParser.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Milestone struct and milestones field to Roadmap</name>
  <files>GSDMonitor/Models/Roadmap.swift</files>
  <action>
Add a `Milestone` struct alongside the existing `Roadmap` struct in Roadmap.swift:

```swift
struct Milestone: Identifiable, Sendable {
    let id: UUID
    let name: String           // "v1.0 MVP", "v1.1 Visual Overhaul"
    let phaseNumbers: [Int]    // Phase numbers belonging to this milestone
    var isComplete: Bool       // True if all phases in group are .done

    init(name: String, phaseNumbers: [Int], isComplete: Bool) {
        self.id = UUID()
        self.name = name
        self.phaseNumbers = phaseNumbers
        self.isComplete = isComplete
    }
}
```

**Important:** `Milestone` is NOT Codable — it is derived at parse time.

Add `var milestones: [Milestone]` to the `Roadmap` struct with a default of `[]`. Do NOT add it to `CodingKeys` — this preserves existing JSON encode/decode. The Roadmap struct needs a new memberwise init that accepts milestones, while still keeping backward compatibility. Update CodingKeys to explicitly exclude milestones.

Provide a custom `init(from decoder:)` that sets `milestones = []` (since it's not in JSON). Keep the existing CodingKeys as-is (only projectName and phases). Add a standard init that takes all fields including milestones.
  </action>
  <verify>Project builds with `swift build` or Xcode build — no Codable errors, no missing initializer errors.</verify>
  <done>Milestone struct exists, Roadmap has milestones field defaulting to empty, CodingKeys unchanged, existing JSON decode unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: Extend RoadmapParser to parse milestone sections</name>
  <files>GSDMonitor/Services/RoadmapParser.swift</files>
  <action>
Add a `parseMilestones` method to `RoadmapParser` and call it in `parse()` to populate `roadmap.milestones`.

**Parsing strategy:** Parse the `## Milestones` section bullet points which have the format:
- `- ✅ **v1.0 MVP** - Phases 1-5 (shipped 2026-02-14) - [archive](...)`
- `- 🚧 **v1.1 Visual Overhaul** - Phases 6-12 (in progress)`

Use regex pattern: `- [✅🚧] \*\*(.+?)\*\* - Phases? (\d+)[-–](\d+)` to extract:
1. Milestone name (e.g., "v1.0 MVP")
2. Start phase number
3. End phase number

Derive `isComplete` from whether ALL phases in the range have `.done` status (check against the already-parsed `mergedPhases` array).

**Integration in parse():** After merging phases (line ~39), call `parseMilestones(from: content, phases: mergedPhases)` and pass the result to the Roadmap init:

```swift
let milestones = parseMilestones(from: content, phases: mergedPhases)
return Roadmap(projectName: walker.projectName, phases: mergedPhases, milestones: milestones)
```

**Graceful degradation:** If no `## Milestones` section exists (e.g., projects without milestone structure), return empty array — timeline simply won't render.
  </action>
  <verify>Project builds. Add a mental check: the ROADMAP.md format `- ✅ **v1.0 MVP** - Phases 1-5` matches the regex. For projects without ## Milestones, milestones should be empty.</verify>
  <done>RoadmapParser.parse() returns Roadmap with populated milestones array. Two milestones parsed from this project's ROADMAP.md: v1.0 MVP (phases 1-5, complete) and v1.1 Visual Overhaul (phases 6-12, not complete).</done>
</task>

</tasks>

<verification>
1. `swift build` succeeds with no errors
2. Roadmap struct still Codable-compliant (milestones excluded from CodingKeys)
3. RoadmapParser produces 2 milestones for this project's ROADMAP.md format
4. Milestone.isComplete correctly reflects phase statuses
</verification>

<success_criteria>
- Milestone struct exists in Roadmap.swift with Identifiable + Sendable conformance
- Roadmap.milestones populated by parser, defaulting to [] for Codable decode
- parseMilestones correctly extracts milestone name, phase range, and completion status
- No runtime crashes on project load
</success_criteria>

<output>
After completion, create `.planning/phases/12-milestone-timeline/12-01-SUMMARY.md`
</output>
