---
phase: 04-dashboard-ui-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GSDMonitor/Models/Project.swift
  - GSDMonitor/Services/ProjectService.swift
  - GSDMonitor/Views/Components/StatusBadge.swift
  - GSDMonitor/Utilities/PreviewData.swift
autonomous: true

must_haves:
  truths:
    - "Project model carries parsed requirements and plans arrays"
    - "ProjectService parses REQUIREMENTS.md and PLAN.md files into Project"
    - "StatusBadge is a shared component accessible from any view"
  artifacts:
    - path: "GSDMonitor/Models/Project.swift"
      provides: "Project with requirements and plans properties"
      contains: "var requirements"
    - path: "GSDMonitor/Services/ProjectService.swift"
      provides: "Full parsing pipeline including requirements and plans"
      contains: "requirementsParser.parse"
    - path: "GSDMonitor/Views/Components/StatusBadge.swift"
      provides: "Shared StatusBadge view for phase status display"
      contains: "struct StatusBadge"
  key_links:
    - from: "GSDMonitor/Services/ProjectService.swift"
      to: "GSDMonitor/Models/Project.swift"
      via: "parseProject populates requirements and plans"
      pattern: "requirements.*requirementsParser"
---

<objective>
Extend Project model with requirements and plans data, wire parsing into ProjectService, and extract StatusBadge as a shared component.

Purpose: Phase cards, requirement badges, and phase detail views all need requirements and plans data on the Project model. StatusBadge is currently private to DetailView but needed by multiple dashboard views.
Output: Project model with full data, ProjectService with complete parsing, shared StatusBadge component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GSDMonitor/Models/Project.swift
@GSDMonitor/Models/Requirement.swift
@GSDMonitor/Models/Plan.swift
@GSDMonitor/Services/ProjectService.swift
@GSDMonitor/Services/RequirementsParser.swift
@GSDMonitor/Services/PlanParser.swift
@GSDMonitor/Views/DetailView.swift
@GSDMonitor/Utilities/PreviewData.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requirements and plans to Project model and wire parsing in ProjectService</name>
  <files>GSDMonitor/Models/Project.swift, GSDMonitor/Services/ProjectService.swift, GSDMonitor/Utilities/PreviewData.swift</files>
  <action>
1. In Project.swift, add two new optional properties:
   - `var requirements: [Requirement]?`
   - `var plans: [Plan]?`
   Update CodingKeys, init(from decoder:), encode(to:), and the manual initializer to include both.

2. In ProjectService.swift, update `parseProject(at:scanSource:)` to:
   - Parse REQUIREMENTS.md using `requirementsParser.parse(content)` and store result
   - Scan for PLAN.md files in `.planning/phases/` subdirectories using FileManager enumerator. Each phase directory (e.g., `01-foundation-models/`) may contain `*-PLAN.md` files. Parse each with `planParser.parse(content)` and collect into array.
   - Pass both `requirements` and `plans` to Project initializer

   Specifically for plans scanning:
   ```
   let phasesDir = planningURL.appendingPathComponent("phases")
   var plans: [Plan] = []
   if let enumerator = FileManager.default.enumerator(at: phasesDir, includingPropertiesForKeys: nil) {
       for case let fileURL as URL in enumerator where fileURL.lastPathComponent.hasSuffix("-PLAN.md") {
           if let content = readFileContent(at: fileURL),
              let plan = planParser.parse(content) {
               plans.append(plan)
           }
       }
   }
   ```

3. In PreviewData.swift, add sample requirements and plans to the full project:
   - Add 3 sample requirements: `Requirement(id: "UI-01", category: "UI", description: "...", mappedToPhases: [1], status: .validated)` etc.
   - Add 2 sample plans: one done (phase 1, plan 1) and one in-progress (phase 2, plan 3) with sample tasks
   - Pass both to the full Project initializer
  </action>
  <verify>Build the project with `xcodebuild -project GSDMonitor.xcodeproj -scheme GSDMonitor build 2>&1 | tail -5` — must compile without errors. Verify Project model has requirements and plans properties.</verify>
  <done>Project model includes requirements and plans arrays. ProjectService parses REQUIREMENTS.md and all PLAN.md files. PreviewData includes sample requirements and plans. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Extract StatusBadge as shared component and update DetailView</name>
  <files>GSDMonitor/Views/Components/StatusBadge.swift, GSDMonitor/Views/DetailView.swift</files>
  <action>
1. Create `GSDMonitor/Views/Components/StatusBadge.swift`:
   - Move the StatusBadge struct from DetailView.swift to this new file
   - Make it `internal` (not private) so it is accessible from any view
   - Keep the exact same implementation: HStack with Circle + Text, statusColor, statusText, padding, background, cornerRadius
   - Add the file to the Xcode project (it will be auto-discovered if in the correct folder within the GSDMonitor target source group)

2. In DetailView.swift:
   - Remove the private `StatusBadge` struct (it now lives in Components/)
   - The PhaseRow can keep referencing `StatusBadge(status:)` since it is now in the same module
   - Verify the file still compiles

Note: Create the `Components/` directory under `GSDMonitor/Views/` if it does not exist. The file must be added to the Xcode project's GSDMonitor target. Use the project.pbxproj or verify Xcode auto-discovers it.
  </action>
  <verify>Build with `xcodebuild -project GSDMonitor.xcodeproj -scheme GSDMonitor build 2>&1 | tail -5` — must compile without errors. StatusBadge is accessible from both DetailView and any future view.</verify>
  <done>StatusBadge is a shared view in Components/StatusBadge.swift. DetailView references it without private declaration. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `xcodebuild build` succeeds
2. Project model has `requirements: [Requirement]?` and `plans: [Plan]?` properties
3. ProjectService.parseProject populates requirements and plans from files
4. StatusBadge is importable from any view in the GSDMonitor module
5. PreviewData compiles with sample requirements and plans
</verification>

<success_criteria>
- Project model carries requirements and plans arrays
- ProjectService parses REQUIREMENTS.md and PLAN.md files during project loading
- StatusBadge is a shared component in Views/Components/
- All existing functionality preserved (no regressions)
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-ui-visualization/04-01-SUMMARY.md`
</output>
