---
phase: 02-file-discovery-parsing
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "App auto-discovers all .planning/ directories in ~/Developer and displays project names in sidebar"
    - "User can manually add project folders outside ~/Developer via file picker"
    - "Clicking a project shows parsed roadmap data (phases, goals, requirements) in content area"
    - "App correctly parses ROADMAP.md, STATE.md, REQUIREMENTS.md, PLAN.md, and config.json from real GSD projects"
    - "Security-scoped bookmarks persist file access across app restarts without re-prompting user"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 2 success criteria with a human-in-the-loop checkpoint. Build, run, and test the app against real GSD projects.

Purpose: Phase 2 involves file system access, security-scoped bookmarks, and UI rendering of parsed data. These require visual verification and real-world testing that automated builds cannot catch.
Output: Verified app meeting all Phase 2 success criteria.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-file-discovery-parsing/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run automated verification</name>
  <files></files>
  <action>
    Run the following automated checks:

    1. Build project: `xcodebuild -project GSDMonitor.xcodeproj -scheme GSDMonitor build`
       - Must succeed with zero warnings

    2. Run tests: `xcodebuild test -project GSDMonitor.xcodeproj -scheme GSDMonitor -destination 'platform=macOS'`
       - All parser tests must pass

    3. Verify all Phase 2 service files exist:
       - GSDMonitor/Services/BookmarkService.swift
       - GSDMonitor/Services/ProjectDiscoveryService.swift
       - GSDMonitor/Services/RoadmapParser.swift
       - GSDMonitor/Services/StateParser.swift
       - GSDMonitor/Services/ConfigParser.swift
       - GSDMonitor/Services/RequirementsParser.swift
       - GSDMonitor/Services/PlanParser.swift
       - GSDMonitor/Services/ProjectService.swift

    4. Verify entitlements include bookmark scope

    5. Check Swift 6 strict concurrency: grep for SWIFT_STRICT_CONCURRENCY = complete in project.pbxproj

    6. Report results clearly for human verification step.
  </action>
  <verify>All automated checks pass.</verify>
  <done>Build succeeds, all tests pass, all files exist, entitlements correct, Swift 6 strict concurrency enabled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 2 implementation</name>
  <action>
    Verify complete Phase 2: File Discovery and Parsing implementation including:
    - Auto-discovery of GSD projects in ~/Developer
    - Parsing of ROADMAP.md, STATE.md, REQUIREMENTS.md, PLAN.md, config.json
    - Sidebar with grouped projects and progress bars
    - Manual project addition via file picker and removal via context menu
    - Security-scoped bookmarks for persistent file access
    - Parsed roadmap data display in detail view

    Steps to verify:
    1. Launch the app (Cmd+R in Xcode or open the built .app)
    2. Check sidebar auto-discovery:
       - The sidebar should show projects found in ~/Developer that have .planning/ directories
       - Projects should be grouped by source (e.g., "~/Developer" section)
       - Each project should show name + small progress bar
       - Verify gsd-monitor itself appears (it has .planning/)
    3. Click a discovered project:
       - Detail view should show the project name and parsed roadmap phases
       - Each phase should show number, name, goal, and status
       - Verify phases match what's in the project's ROADMAP.md
    4. Test manual project addition:
       - Click the plus (+) button in the sidebar toolbar
       - Select a project folder that has .planning/ (e.g., any GSD project)
       - Verify it appears under "Manually Added" section in sidebar
    5. Test manual project removal:
       - Right-click the manually added project
       - Select "Remove from GSD Monitor"
       - Verify it disappears from sidebar
    6. Test bookmark persistence:
       - Add a project manually (step 4)
       - Quit the app completely (Cmd+Q)
       - Re-launch the app
       - Verify the manually added project still appears without re-prompting for file access
    7. Test edge cases:
       - Verify app starts with no project selected (empty detail state)
       - Verify clicking plus and canceling the file picker does nothing
       - Check that non-GSD folders (without .planning/) are not shown
  </action>
  <verify>All 7 verification steps pass. User confirms "approved".</verify>
  <done>Phase 2 success criteria verified by human. App discovers, parses, and displays GSD projects correctly.</done>
</task>

</tasks>

<verification>
Phase 2 Success Criteria (from ROADMAP.md):
1. App auto-discovers all .planning/ directories in ~/Developer and displays project names in sidebar
2. User can manually add project folders outside ~/Developer via file picker
3. Clicking a project shows parsed roadmap data (phases, goals, requirements) in content area
4. App correctly parses ROADMAP.md, STATE.md, REQUIREMENTS.md, PLAN.md, and config.json from real GSD projects
5. Security-scoped bookmarks persist file access across app restarts without re-prompting user
</verification>

<success_criteria>
- All 5 Phase 2 success criteria verified by human
- Build passes with zero warnings
- All parser tests pass
- No crashes or unexpected behavior during manual testing
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-discovery-parsing/02-05-SUMMARY.md`
</output>
